import {
  FETCH_LIST,
  FETCH_ITEM,
  CREATE_ITEM,
  EDIT_ITEM,
  DELETE_ITEM
} from "../constants/types";

export default (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
